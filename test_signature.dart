import 'package:flutter_local_notifications/flutter_local_notifications.dart';
void main() {
  // Use reflection to get types if possible, or just generate a compile error
  // Wait, test_signature.dart needs to be compiled to show errors.
}
